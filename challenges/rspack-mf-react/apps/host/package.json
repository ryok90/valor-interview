{
  "name": "host",
  "version": "1.0.0",
  "scripts": {
    "dev": "pnpm typecheck && rspack dev",
    "build": "pnpm typecheck && rspack build",
    "preview": "rspack preview",
    "typecheck": "tsc --noEmit"
  },
  "zephyr:dependencies": {
    "remote": "zephyr:remote@*"
  }
}
